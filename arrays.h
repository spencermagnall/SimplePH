#ifndef ARRAYS_H
#define ARRAYS_H

#include "const.h"

struct arrays
{
    float x[nopart];
    float v[nopart];
    float m[nopart];
    float h[nopart];
    float rho[nopart];
    float u[nopart];
    float P[nopart];
    float cs[nopart];

};

#endif 
